PORT=8080
DATABASE_URL=postgres://helpdesk:helpdesk_dev@localhost:5432/helpdesk?sslmode=disable
JWT_SECRET=dev-secret-change-in-prod

# Email (SMTP). Leave SMTP_HOST empty to log emails to stdout instead.
# Mailpit (dev) runs on localhost:1025 with a web UI on http://localhost:8025
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=Help Desk <helpdesk@fintara.com>
